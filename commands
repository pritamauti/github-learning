git config --global user.email "pritam.auti@gmail.com"
git config --global user.name "pritam auti"
echo "# github-learning" >> README.md
git init
git add [filename] || -A
git commit -m "msg"
git remote add origin https://github.com/pritamauti/github-learning.git
git push origin master

for user if we want to 
